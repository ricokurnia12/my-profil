// next.config.js
module.exports = {
  typescript: {
    // ⚠️ Only do this if absolutely necessary
    ignoreBuildErrors: true
  },
  eslint: {
    // ⚠️ Only do this if absolutely necessary
    ignoreDuringBuilds: true
  }
};
